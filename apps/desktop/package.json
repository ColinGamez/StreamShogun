{
  "name": "@stream-shogun/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "tsc -p tsconfig.json && electron-builder --config electron-builder.yml",
    "build:win": "tsc -p tsconfig.json && electron-builder --config electron-builder.yml --win",
    "build:mac": "tsc -p tsconfig.json && electron-builder --config electron-builder.yml --mac",
    "build:linux": "tsc -p tsconfig.json && electron-builder --config electron-builder.yml --linux",
    "build:unpackaged": "tsc -p tsconfig.json",
    "clean": "node -e \"const fs=require('fs');['dist','out','release'].forEach(d=>{fs.rmSync(d,{recursive:true,force:true})})\""
  },
  "dependencies": {
    "@stream-shogun/core": "workspace:*",
    "@stream-shogun/shared": "workspace:*",
    "better-sqlite3": "^12.6.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "electron": "^31.0.0",
    "electron-builder": "^24.13.0",
    "typescript": "^5.4.0"
  }
}
